function runTest(name,status,duration){
//console.log(`${name},${status},${duration} ms`)
return`${name},${status},${duration} ms`
}

runTest("Login","Pass",10);  //No Result  //We need to assign this 